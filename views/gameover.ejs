<!DOCTYPE html>
<html>
<head>
  <title>Game Over</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="game-over">
    <h1>Game Over!</h1>
    <p><%= message %></p>
    <p>Final Score: <span class="final-score"><%= game.score %></span></p>
    
    <!-- New form to save player name -->
    <form action="/scores" method="POST">
      <input type="text" name="playerName" placeholder="Enter your name" required>
      <input type="hidden" name="gameId" value="<%= game._id %>">
      <input type="hidden" name="score" value="<%= game.score %>">
      <button type="submit">Save Score</button>
    </form>
    
    <div class="actions">
      <a href="/games/new">New Game</a>
    </div>
  </div>
</body>
</html>